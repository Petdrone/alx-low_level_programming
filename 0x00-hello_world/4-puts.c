#include <stdio.h>
/**
 *main- Servies as the entry point for the program
 *
 *Return: By default returns zero if no erros otherwise returns non-zero value.
 */
int main()
{
puts("\"Programming is like building a muiltilingual puzzle");
return (0);
}
